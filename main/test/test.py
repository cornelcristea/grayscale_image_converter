# create 'target' folder in repo if not exist
# copy main.py in target and convert it to exe (maybe venv is needed)
# call exe file to convert an image
# compare the image generated with the reference one